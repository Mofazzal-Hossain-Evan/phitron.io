#include<stdio.h>

int main(){
    int a,b, sum, minus;
   // printf("Enter two numbers: ");
    scanf("%d %d", &a,&b);
    if(a-b>=1 ){
        sum= a-b;
        printf("%d", sum);
    } else {
      sum==0;
        printf("%d", sum);
    }
    
}